# Агро-Групп — сайт

Официальный сайт ООО «Агро-Групп». Next.js 15 + TypeScript + Tailwind CSS.

## Разработка

```bash
npm install
npm run dev      # http://localhost:3000
npm run build    # прод-сборка
npm run lint     # проверка
```

## Структура и правила

См. `CLAUDE.md` и `claude_rules/`.
